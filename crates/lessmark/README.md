# lessmark

Reserved Rust crate for Lessmark.

Lessmark is a strict, agent-readable document format inspired by Markdown. The reference implementation currently lives in the npm `lessmark` package. This crate reserves the name for the official Rust implementation.