# lessmark

Reserved Python package for Lessmark.

Lessmark is a strict, agent-readable document format inspired by Markdown. The reference implementation currently lives in the npm `lessmark` package. This Python package reserves the name for the official Python implementation.

It currently exposes package metadata only.