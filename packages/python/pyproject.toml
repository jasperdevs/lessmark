[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lessmark"
version = "0.1.5"
description = "Readable markup with typed blocks and a stable JSON AST."
readme = "README.md"
requires-python = ">=3.9"
license = "MIT"
authors = [{ name = "Jasper" }]
keywords = ["lessmark", "markdown", "ast", "agent", "parser", "cli"]
classifiers = [
  "Development Status :: 4 - Beta",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Topic :: Text Processing :: Markup"
]

[project.scripts]
lessmark = "lessmark.cli:main"

[project.urls]
Homepage = "https://lessmark.org"
Repository = "https://github.com/jasperdevs/lessmark"
Issues = "https://github.com/jasperdevs/lessmark/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
lessmark = ["schemas/*.json"]
