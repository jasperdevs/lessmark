# @jasperdevs/lessmark

Scoped npm package for Lessmark.

This package re-exports the unscoped `lessmark` package so the project can support both names without maintaining two parser implementations.

```js
import { parseLessmark } from "@jasperdevs/lessmark";
```