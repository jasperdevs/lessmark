# @jasperdevs/lessmark-cli

Command-line tools for Lessmark files.

```sh
lessmark parse file.lmk
lessmark check file.lmk
lessmark format file.lmk
```

This package provides the `lessmark` binary. The parser and formatter live in the `lessmark` package.